services:
  frontend:
    type: static
    cache-path: frontend/node_modules
    cache-key: frontend/yarn.lock
    assets: frontend/build
    runtime: reactjs
    commands:
      - cd frontend
      - yarn install
      - yarn build
