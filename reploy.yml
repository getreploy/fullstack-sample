services:
  frontend:
    image: reploy/node
    port: 3000
    path: frontend
    build:
      - yarn cache dir
      - cd ~
      - pwd
      - yarn install --frozen-lockfile
      - yarn build
    serve:
      - npx serve build/ -l 3000
    environment:
      SKIP_PREFLIGHT_CHECK: true
  backend:
    port: 5000
    runtime: go
    path: backend
    build: 
      - go build -o reploy
    serve:
      - ./reploy
  redis:
    do-not-expose: true
    port: 6379
    runtime: redis
